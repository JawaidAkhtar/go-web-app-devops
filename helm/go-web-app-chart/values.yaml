# Default values for go-web-app-chart.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: jawaid365/go-web-app
  pullPolicy: IfNotPresent
  tag: "12211294454"

ingress:
  enabled: true
  className: "nginx"
  annotations:
    kubernetes.io/ingress.class: nginx
    kubernetes.io/tls-acme: "true"
  hosts:
    - host: ac2d4dcfec55342e89b464dd5d8bec57-cd85588270b339a7.elb.us-east-1.amazonaws.com
      paths:
        - path: /
          pathType: Prefix
          backend:
            service:
              name: go-web-app
              port:
                number: 80

